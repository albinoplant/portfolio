import React from 'react';
import { Link } from 'react-router-dom';

const Projects = () => (
    <div>
        diferent example
        <Link to="/">BACK</Link>
    </div>

    );
export default Projects;